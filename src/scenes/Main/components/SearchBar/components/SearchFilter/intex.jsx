import React, { Fragment } from 'react';

const SearchFilter = () => (
    <Fragment>
        <button>TITLE</button>
        <button>GENRE</button>
    </Fragment>
);

export default SearchFilter;
